import { StyleSheet } from 'react-native';

export const home = StyleSheet.create({
  header: {
    backgroundColor: 'red',
    fontSize: 20,
    padding: 10,
    display: 'flex',
    justifyContent: 'center',
    marginTop: 10,
    color: 'white',
    borderRadius: 12,
    minWidth: 200,
    textAlign: 'center',
  },
});
